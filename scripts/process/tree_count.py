class TreeNumberCount:
    def __init__(self, data_path):
        self.data_path = data_path

    def process(self):
        # 示例：根据数据生成或获取树木ID，此处返回一个示例ID
        tree_id = "Tree-001"
        print(f"Tree number processed: {tree_id}")
        return tree_id